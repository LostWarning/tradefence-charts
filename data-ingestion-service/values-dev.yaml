# Local k3d configuration for API Gateway

appName: data-ingestion-service
namespace: app
environment: dev

# Application configuration
config:
  logLevel: "DEBUG"

internalServices:
  telemetryEndpoint: dev-tempo.monitoring:4317