apiVersion: v1
kind: Service
metadata:
  name: {{ include "trigger-scheduler.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "trigger-scheduler.labels" . | nindent 4 }}
  annotations: {}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range $name, $config := .Values.service.ports }}
    - port: {{ $config.port }}
      targetPort: {{ $config.targetPort }}
      protocol: {{ $config.protocol }}
      name: {{ $name }}
    {{- end }}
  selector:
    {{- include "trigger-scheduler.selectorLabels" . | nindent 4 }}
