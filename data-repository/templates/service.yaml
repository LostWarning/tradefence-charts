apiVersion: v1
kind: Service
metadata:
  name: {{ include "data-repository.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "data-repository.labels" . | nindent 4 }}
  annotations: {}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range $name, $config := .Values.service.ports }}
    - port: {{ $config.port }}
      targetPort: {{ $config.targetPort }}
      protocol: {{ $config.protocol }}
      name: {{ $name }}
    {{- end }}
  selector:
    {{- include "data-repository.selectorLabels" . | nindent 4 }}
