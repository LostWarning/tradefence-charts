# Dev k3d configuration for API Gateway

appName: api-gateway
namespace: app
environment: dev

# Ingress configuration
ingress:
  enabled: true
  ingressClassName: traefik
  hosts:
    - host: api.dev.tradefence.in
      paths:
        - path: /
          pathType: Prefix
  tls:
    - secretName: api-dev-tradefence-tls
      hosts:
        - api.dev.tradefence.in
  annotations:
    # WebSocket sticky sessions
    traefik.ingress.kubernetes.io/service.sticky.cookie: "true"
    traefik.ingress.kubernetes.io/service.sticky.cookie.name: "gateway-session"
    traefik.ingress.kubernetes.io/service.sticky.cookie.httponly: "true"
    traefik.ingress.kubernetes.io/service.sticky.cookie.secure: "true"
    traefik.ingress.kubernetes.io/service.sticky.cookie.samesite: "strict"

# Application configuration
config:
  logLevel: "DEBUG"
